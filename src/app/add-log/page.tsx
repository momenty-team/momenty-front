import AddLog from '@/feature/add-log';
import { suitFont } from '@/styles/font';

function AddLogFunnel() {
  return (
    <main className={`h-full ${suitFont.className}`}>
      <AddLog />
    </main>
  );
}

export default AddLogFunnel;
